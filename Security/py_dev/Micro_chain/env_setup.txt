--- Install required packages:

[required] 		sudo apt-get install -y cmake git libssl-dev libffi-dev
[sqlite3]		sudo apt-get install sqlite3 libsqlite3-dev
[python2]  		sudo apt-get install -y python-pip python-dev python-setuptools
[python3]  		sudo apt-get install -y python3-pip python3-dev python3-setuptools
[pip]			sudo python -m pip install --upgrade pip && sudo python3.5 -m pip install --upgrade pip

--- Add requirements
#				sudo python3.5 -m pip install -r requirements.txt
[Flask]			sudo python3.5 -m pip install flask==1.0.2
[cryptography]	sudo python3.5 -m pip install cryptography==2.6.1
[pycrypto]		sudo python3.5 -m pip install pycrypto==2.6.1
